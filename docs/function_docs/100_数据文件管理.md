# 数据文件管理 功能逻辑 & 开发文档

> 文档元信息（每次修改必须更新）
>
> | 项 | 内容 |
> |----|------|
> | 功能编号 | 100 |
> | 所属模块 | 数据文件 |
> | 主要代码位置 | 后端 `model/data_doc.py`、`serv/serv_data_doc.py`、`serv/data_doc_templates.py`、`api/api_data_doc.py`、`scripts/data_stats/`；前端 `pages/model_doc/DataDocs.tsx`、`DataDocDetail.tsx`、`DataRecordDocDetail.tsx`、`DataEnvDocDetail.tsx`、`DataDocPage.tsx`、`DataDocTypes.ts`、`gridSpans.ts`、`api/ApiDataDoc.ts`、`pages/Home.tsx` 数据菜单 |
> | 文档版本 | v1.62 |
> | 最后更新 | 2026-09-17 |

---

# 1. 功能概述

- **用途**：按「产品 + 文档类型 + 文档版本」维护数据侧 Word/Excel，交互与模型文件相同。记录 / 反馈 / 统计表编辑页对齐模型文件训练/测试记录：无左侧章节，单页原表，导出一张 xlsx（不导出封面/修订）。`dd_016` / `dd_017` 编辑页对齐模型文件 `md_019`：无左侧章节，单页资产表 + 只读说明 + 周检表，导出 Word。
- **使用场景**：用户从左侧菜单「数据文件」进入二级分组后再进某一子菜单，对该类型文档建档、改章节、导出。分组按归档：规范、采集记录、人员记录、入库记录、数据资产、设备与环境维护。名称与 `doc_type` 不改。
- **最终目标**：`(product_id, doc_type, version)` 唯一；封面空的编制部门为「数据部」；无独立「产品信息」章，不回写产品主数据。
- **导出格式**：`docx` 走封面→修订→目录→正文；`dd_016` / `dd_017` 按 DEM 式周检表导出；记录 / 统计表 `xlsx` 一张原表（多工作表按原表名，不含封面/修订）；其余 `xlsx` 每章节一张工作表。
- **关联**：引擎与规则对齐 [99_模型文件管理.md](./99_模型文件管理.md)；权限 `data_doc_view` / `data_doc_edit`。`dd_016_qr` / `dd_017_qr` 仍保留类型供旧数据，不出现在菜单（周检表并入对应说明 Word）。

菜单分组（类型名不变；路由仍为 `/data_docs/:type`）：

| 二级导航 | 文档 | doc_type | 格式 |
|----------|------|----------|------|
| 规范 | 数据采集需求、数据标注需求、数据标注规则、标记规则、标注质量评估方法、人员考核评价方法 | `dd_001` / `md_003` / `md_002_01` / `md_002_02` / `dd_006` / `dd_007` | docx |
| 采集记录 | 多中心数据回传记录、数据整理记录、数据采集需求反馈 | `dd_002` / `dd_003` / `dd_004` | xlsx |
| 人员记录 | 人员培训记录 ×2、考核记录 ×7 | `dd_005_01` / `dd_005_02` / `dd_013_01`～`dd_013_07` | xlsx |
| 入库记录 | 试标注 ×2、标注记录 ×3、标注需求反馈、数据库上传记录、查重记录 | `dd_008_01` / `dd_008_02` / `dd_009_01`～`dd_009_03` / `dd_011` / `dd_010` / `dd_012` | xlsx |
| 数据资产 | 数据库维护记录、原始/基础/标注数据库统计表 | `dd_014` / `dd_015_01` / `dd_015_02` / `dd_015_03` | xlsx |
| 设备与环境维护 | 数据设备清单、开发环境维护记录说明、标注环境维护记录说明 | `dd_eq` / `dd_016` / `dd_017` | xlsx / docx（016、017） |

`数据/` 中与 QR 表结构相同的 `DD-016-A0` / `DD-017-A0` 记录 xlsx 不另开类型。旧类型 `dd_016_qr` / `dd_017_qr` 不进菜单。`md_002_01` / `md_002_02` / `md_003` 类型码仍用 MD 编号（与样例 Word 文件编号一致），菜单挂在数据文件；`dd_002` / `dd_003` 仍是 Excel。

大表（标注明细等）只预填表头与少量空行。`dd_015_*` 模板只留表头空表，不含样例数字。统计数字由「数据统计」选完病例文件夹后，按当前所选产品写入该产品最新对应 `dd_015_*` 记录表（原始→`dd_015_01`，基础→`dd_015_02`，标注→`dd_015_03`）；同一记录章节同时写入病例明细（`case_rows` / `case_meta`，编辑页不展示、导出仍只出数据分布原表）；封面/修订不改。同一批识别结果写入该产品已有试标注 `dd_008_*`、标注记录 `dd_009_01` / `dd_009_02`：PID（DICOM PatientID，空则病例文件夹名）；标注日期/审核日期←时间线「数据部」匹配该类型的最早/最晚一天；标注人员←参与人员职能「标注人员」轮流；审核医生←职能「审核医生」轮流（没有则空）；标注项目/数据类型由类型定；可用状态=已标注；审核结果=通过。`dd_009_03`：评测日期←数据部时间线分诊最早一天；评测项目=肺栓塞分诊；数据类型=测试集；测试医生1/2←前两名标注人员；二合一=一致；仲裁医生←职能「仲裁医生」轮流（无则空）；仲裁时间←数据部分诊最晚一天（无则评测日期）；仲裁状态=无需仲裁（二合一已一致，不编造需仲裁）。封面/修订不改。无该文档则提示先新增，不自动建档。打开/换产品/导出「数据标注需求」`md_003` 时：两段「数据」正文「训练数据量要求 / 测试数据量要求 / 调优数据量要求」数字←该产品最新 `dd_010` 标注库按数据集名精确匹配（肺叶段←肺叶分割训练集/测试集/调优集，肺栓塞段←肺栓塞分割训练集、肺栓塞分诊测试集、肺栓塞分割调优集），无 `dd_010` 则读最新 `dd_012` 同名批次；写成「N例左右」，「左右」必须保留；无对应数据集则该行不改。交付时间、任务分类/描述、封面/修订不改。打开/换产品「标注需求反馈」`dd_011` 时：文档需求标注类型/数据量/交付时间←该产品最新 `md_003`「数据」段；实际情况标注类型/数据量/交付时间←最新 `dd_010` 标注库表（按数据集名精确匹配，无则 `dd_012` 同名批次）；`44943` 这类 Excel 序列写成 `yyyy.M.d`。封面/修订不改。无源不编造。打开/换产品「多中心数据回传记录」`dd_002` 时：数据来源/医院编号←该产品合规医院列表（对方单位名称/医院编号）；按医院编号优先匹配已有行，匹配则保留负责人/数量；清单有、表没有的补空行（数据类型沿用首行）；表有、清单没有的样例行去掉。数据回传方式每行写死「硬盘邮寄」；数据接收时间空格←时间线数据部含「回传」或「多中心」的日期（`yyyy.M.d`，多天按早→晚轮流填空行）；数据采集负责人←参与人员职能「数据采集人员」轮流（已有姓名按最新名单覆盖）；无该职能则先按 48 默认 6 人写入参与人员再填。封面/修订不改。无清单不改。打开/换产品「数据整理记录」`dd_003` 时：采集单位←该产品最新 `dd_002`「数据来源」；脱敏检查、清洗日期←「数据接收时间」；数据量←「回传数据量」（空则「实际接收量」）；脱敏检查、清洗人员←参与人员职能「脱敏+清洗人员」轮流（无该职能则该列沿用已有完整行）。按医院名匹配已有行，只改这三列 + 清洗人员；回传有、整理没有的补行（所属项目/清洗人员/检查方式/采集设备/采集协议/数据来源场景/评估结论/签字沿用已有最后一条完整行，完整行=有采集设备或采集协议或评估结论，清洗人员有职能则改用职能轮流）；已有补行这几列空则按完整行补全。整理有、回传没有的样例行去掉。无 `dd_002` 或回传无医院行则不改。封面/修订不改。打开/换产品「数据采集需求反馈」`dd_004` 时：数据量文档需求←该产品最新 `dd_001`「总计需要采集至少 N 例」写成「总计不少于 N 例数据」；数据量实际情况←最新 `dd_002` 回传数据量（空则实际接收量）合计写成「总计约 N 例」；数据来源实际情况←回传医院行数写成「来源于 N 家医院」。检查方式/性别/年龄/地域/设备/协议/参数/精度/是否符合/问题/结论/签字不改。无 `dd_002` 则两处实际情况不改；无 `dd_001` 则文档需求数据量不改。封面/修订不改。`dd_005_01` / `dd_005_02` 讲解人员、记录人编辑格为下拉：选项←当前产品参与人员职能「标注人员」姓名去重；单击选中后写入并保存。打开/换产品时「参与培训人员名单」「考核通过人员名单」←全部标注人员，「审核人员」←全部审核医生（每行 5 名，行数随人数增减；无该块则补上）。其它培训格与其它文档「记录人」签字格仍手写。无对应职能则该块空。`dd_013_01`～`dd_013_04` 打开/换产品「参与考核人员」←全部标注人员（每行 5 名），「参与考核人数」←人数；「考核时间」←时间线「数据部」含「考核」且含本表项目（肺叶/肺栓塞）的日期，无则用「人员培训」或「培训记录」+同一项目（`yyyy.M.d`；初次最早一天，定期多天换行）；无匹配日不改。考核要点/细则不改。`dd_013_05`～`dd_013_07` 日常表按标注人员一人一行（匹配则保留已填态度/考勤/分数/日期），审核医生←审核医生轮流；空的打分格按原表规则随机填：工作态度=合格；考勤/专心度/反馈态度/规范评级其余为 A、每行随机 1 列为 B；标注质量分数 87～97（规范评级为 B 则 87～90）。空的日期列←时间线「数据部」：先匹配数据标注记录+本表项目（05 肺栓塞分割不含分诊/试标注，06 肺叶不含试标注，07 分诊不含试标注），无则考核，再无则人员培训/培训记录+同一项目；左列日期按早→晚轮流写成 `yyyy-MM-dd`（对齐原表），右列日期为该日加一天。无匹配日不改。已有日期只统一成该格式、不改日期本身。「标注人员姓名」「审核医生」为下拉单击保存。无对应职能则名单空。打开/换产品「数据库维护记录」`dd_014` 时：检查日期←时间线「数据部」含「数据库维护」的日期（`yyyy.M.d`）；多天按早→晚一张表一天；天数少于现有维护表则去掉多余样例表；天数多于现有表则按最后一张完整表复制补表。检查人员/检查内容勾选/复核人/封面/修订不改。无匹配日不改。编辑页仍可「导入统计 Excel」备用。离线脚本 `trace-api/scripts/data_stats/` 仅作备用。见 [101_数据统计.md](./101_数据统计.md)、[102_合规医院列表.md](./102_合规医院列表.md)。

---

# 2. 前置依赖 & 关联模块

## 2.1 涉及页面

| 页面 / 路由 | 作用 |
|------------|------|
| `/data_docs/:type` | 列表 |
| `/data_docs/:type/edit/:id` | 编辑：一般 Word 为目录树；记录 / 反馈 / 统计表为单页原表（无左侧章节）；`dd_016` / `dd_017` 为 DEM 式单页 |
| `/data_docs/:type/view/:id` | 查看 |

## 2.2 涉及组件

| 组件 / 文件 | 作用 |
|------------|------|
| `pages/model_doc/DataDocs.tsx` | 列表（克隆模型文件列表） |
| `pages/model_doc/DataDocDetail.tsx` | 详情（克隆模型文件编辑页）；首格含「评审记录」的表按 Word 合并格展示 |
| `pages/model_doc/DataDocTypes.ts` | 类型名与左侧二级菜单分组 `DATA_DOC_MENU` |
| `pages/model_doc/DataRecordDocDetail.tsx` | 记录 / 反馈 / 统计表单页（无左侧章节，原表合并格展示） |
| `pages/model_doc/DataDocPage.tsx` | 按 `type` 分流到章节树、记录单页或环境维护说明编辑页 |
| `pages/model_doc/DataEnvDocDetail.tsx` | `dd_016` / `dd_017` 单页编辑（资产表 + 只读说明 + 周检表），对齐模型文件 `md_019` |
| `api/ApiDataDoc.ts` | HTTP |

## 2.3 涉及接口

前缀 `/trace-api/data_doc`，方法与模型文件同名替换 `model`→`data`。权限 `data_doc_edit` / `data_doc_view`。另：`POST /import_stats_excel` 上传统计脚本 xlsx，解析为章节树返回，不落库（需 `data_doc_edit`）。`POST /scan_dicom_stats` 按服务器路径或该产品最新 `dd_010` 存储路径读 DICOM 头（远程 `主机:/abs` 用 SFTP 用户名+密码，密码不落库），返回 JSON 行（需 `data_doc_edit`），见 [101_数据统计.md](./101_数据统计.md)。

## 2.4 状态仓库 / 公共方法

同 99：`serv_review_util`、`docx_util`、`ProductVersionSelect`。封面部门规则 `DOC_DEPT[dd_*]=data`。

统计操作入口见 [101_数据统计.md](./101_数据统计.md)（菜单「数据统计」，浏览器本地计算）。备用离线脚本（全产品，不写死路径）：

| 脚本 | 作用 |
|------|------|
| `scripts/data_stats/data_statistics_multi.py` | `--input` 病例 DICOM 目录 → 病例分布 xlsx |
| `scripts/data_stats/summarize_db_stats.py` | 分布 xlsx → 因素/类别/序列数/占比 |
| `scripts/data_stats/triage_subgroup.py` | 分布 xlsx（可含 gt/pred/dice）→ 亚组统计 xlsx |

编辑页「导入统计 Excel」只替换非封面/修订章节，点保存才落库。

## 2.5 【只读 / 禁止修改】模块清单

| 模块 / 文件 / 函数 | 标记 | 原因 |
|--------------------|------|------|
| `src/store/`、`src/common/`、`src/utils/base/` | 【禁止修改】 | 全局公共 |
| 产品文件、模型文件已上线 Word 行为 | 【禁止修改】 | 本功能只加数据文件与模型 Excel |
| `/product/*`、时间线、参与人员 | 【只读】 | 不回写 |

---

# 3. 完整执行流程（分步细化）

## 3.0 触发入口

菜单「数据文件 → {二级分组} → {类型名}」进入 `/data_docs/{doc_type}`。分组：规范、采集记录、人员记录、入库记录、数据资产、设备与环境维护。规范顺序：采集需求、标注需求、标注规则、标记规则、评估方法、考核评价方法。入库记录顺序：试标、标注、标注需求反馈、上传、查重。设备与环境维护顺序：数据设备清单、开发环境维护记录说明、标注环境维护记录说明。`dd_016_qr` / `dd_017_qr` 不进菜单。类型名称、路由、权限不改。

## 3.1～3.6

与 99 相同：列表 / 新增 / 编辑查看 / 导出 / 复制 / 删除。接口与表名为 `data_doc`。导出按类型 format 输出 docx 或 xlsx。Word 评审表与 99 相同：相邻相同非空格先横向合并，纵向合并首列（该列已横向合并时仍合并）。`dd_015_*` 可导入统计脚本 Excel 替换记录章节。

成功终态：`data_doc` 落库一行；导出不落库。

---

# 4. 核心规则 & 判定条件

| 编号 | 规则 / 判定条件 | 含义 | 不满足 / 命中时的后果 |
|------|----------------|------|----------------------|
| R1 | `(product_id, doc_type, version)` 唯一 | 同产品同类型不重复版本 | `msg_obj_exist` |
| R2 | 写 `data_doc_edit`，读 `data_doc_view` | 权限 | 无权限拦截，菜单不可见 |
| R3 | `product_manager` 非超管 | 只看自己创建的产品 | 其它产品不出现 |
| R4 | 自动获取仅填空 | 保护手改 | 已填保留；打开或换产品时精确全文替换产品名称 / 型号 / 产品代码（规则同 99）；**切换产品时覆盖封面签署人与日期、修订首行日期/修订人/批准人**；附件评审记录评审时间/参评人员/批准人打开/换产品/导出按时间线与参与人员覆盖 |
| R5 | 不回写产品主数据 | 只读来源 | 不影响 `product` |
| R6 | 封面编制部门空则「数据部」 | 与模型文件「模型部」区分 | 用户已填则保留 |
| R7 | 更新忽略 `doc_type` | 类型由菜单锁定 | 不能改类型 |
| R9 | 导入统计 Excel 只替换记录章节 | 保护封面/修订 | 封面、修订保留，其余按工作表重建；不生成「产品信息」章 |
| R17 | `dd_015_*` 记录表由数据统计写入 | 「数据统计」选产品并算完后，写入该产品最新对应统计表的记录表（数据分布：总量/医院数/因素序列数与占比），并在同一记录章节写入病例明细 `case_rows` / `case_meta`。封面/修订不改。编辑页与导出仍只展示/导出数据分布原表。无该文档则提示先新增 | 打开已存非样例表不被打开动作覆盖；再次统计写入会换记录表和 `case_rows`；旧文档无 `case_rows` 时数据统计页明细为空 |
| R18 | 试标注/标注记录由数据统计与时间线/人员写入 | PID←DICOM PatientID（空则病例文件夹名）。`dd_008_*` / `dd_009_01` / `dd_009_02` 同时带：标注日期/审核日期←数据部时间线最早/最晚匹配日；标注人员←职能「标注人员」轮流；审核医生←职能「审核医生」轮流（无则空）；项目/类型由文档类型定；可用状态=已标注；审核结果=通过。`dd_009_03`：评测日期/项目/测试集/测试医生1·2/二合一=一致；仲裁医生←职能「仲裁医生」轮流；仲裁时间←分诊最晚一天；仲裁状态=无需仲裁。打开记录页若表与来源不一致则写入。封面/修订不改。无文档不建档 | 无时间线/无标注人员则对应格空；无审核医生职能则审核医生空；无仲裁医生职能则仲裁三列空 |
| R19 | `dd_011` 需求/实际数据量与交付时间自动获取 | 打开/换产品覆盖标红格：文档需求「2.肺叶分割」及数据量、交付时间←该产品最新 `md_003`「数据」段（肺叶/肺栓塞分列；交付写成 `yyyy.M.d`）；实际情况「2.肺叶分割」及数据量、交付时间←最新 `dd_010` 标注库按数据集名精确匹配（肺栓塞分割训练集/调优集/分诊测试集，肺叶分割训练/调优/测试；交付←上传日期最晚一天，无则同文档需求日期）。无 `dd_010` 则读最新 `dd_012` 同名批次。纯数字 Excel 序列转成日期。封面/修订及性别/年龄/地域/设备/人员/流程/问题/结论/是否符合不改 | 无对应数据段或数据集则该格不编造；无日期则只转序列、不另写 |
| R27 | `md_003` 训练/测试/调优数据量自动获取 | 打开/换产品/导出覆盖两段「数据」中「训练数据量要求」「测试数据量要求」「调优数据量要求」的数字：←该产品最新 `dd_010` 标注库按数据集名精确匹配（肺叶段←肺叶分割训练集/测试集/调优集，肺栓塞段←肺栓塞分割训练集、肺栓塞分诊测试集、肺栓塞分割调优集），无 `dd_010` 则读最新 `dd_012` 同名批次。格式写成「训练数据量要求：N例左右」等，「左右」必须保留；其余正文（含交付时间、任务分类/描述）不改。封面/修订不改 | 无对应数据集则该行不改、不编造 |
| R28 | 数据 Word 章节正文/图/表交错 | 编辑页与导出对齐自研软件研究报告与 99 R33：按正文「图N」插入 `images`，按「见下表/表N」插入表格。`md_002_01`「标注规则及示例」、`md_002_02`「标记示例」图从 `src-res/doc_figures/` 补，章节 `images` 空才填。封面/修订/记录表不改 | 无原图则该章不编造；已有 `images` 不覆盖 |
| R26 | `dd_014` 检查日期从时间线带出 | 打开/换产品：检查日期←时间线「数据部」含「数据库维护」的日期，写成 `yyyy.M.d`。多天按早→晚一张表一天；天数少于现有维护表则去掉多余样例表；天数多于现有表则按最后一张完整表复制补表。检查人员/检查内容勾选/复核人/封面/修订不改 | 无匹配日不改 |
| R25 | `dd_013_*` 考核名单与时间自动带出 | 打开/换产品：`dd_013_01`～`04`「参与考核人员」←标注人员去重（每行 5 名），「参与考核人数」←该人数。「考核时间」←时间线「数据部」：先匹配含「考核」且含本表项目（肺叶/肺栓塞）的日期；无则匹配「人员培训」或「培训记录」+同一项目。写成 `yyyy.M.d`；`01`/`02` 初次取最早一天，`03`/`04` 定期按早→晚换行。`dd_013_05`～`07` 日常表一人一行←标注人员（按姓名匹配保留已填态度/考勤/分数/日期），「审核医生」←审核医生轮流；空的打分格按原表规则随机填：工作态度=合格；考勤/工作专心度/对反馈的态度/标记规范程度评级其余为 A、每行随机 1 列为 B；标注质量分数 87～97 整数（规范评级为 B 则 87～90）。空的日期列←时间线「数据部」：先匹配数据标注记录+本表项目（05 肺栓塞分割且不含分诊/试标注，06 肺叶且不含试标注，07 分诊且不含试标注），无则考核，再无则人员培训/培训记录+同一项目；左列按早→晚轮流 `yyyy-MM-dd`（对齐原表），右列为该日加一天。姓名/审核医生为下拉，日期列为日期选择器，单击选中即保存。考核要点/细则/封面/修订不改 | 无标注人员则名单空、不留样例名；无审核医生则审核医生列空；无匹配时间线则考核时间/日常日期不改；已填打分不覆盖；已填日期只统一成 `yyyy-MM-dd` |
| R24 | `dd_005_*` 培训/审核/通过名单从参与人员带出 | 打开/换产品覆盖：参与培训人员名单、考核通过人员名单←职能「标注人员」去重；审核人员←职能「审核医生」去重。每行 5 个姓名，行数随人数增减。`dd_005_01` 若无这三块则在培训结果前补上（考核方式沿用「标注人员对10例数据进行标注，由审核员进行评估。」）。培训日期/人数/内容/结果/签字不改 | 无对应职能则该块只留标签、不写样例名 |
| R23 | `dd_005_*` 讲解人员/记录人从标注人员下拉 | `dd_005_01` / `dd_005_02` 中上一格为「讲解人员」或「记录人」的编辑格改为下拉。选项←当前产品参与人员职能「标注人员」姓名去重。单击选中后写入该格并 `update_data_doc`。只读仍展示文字。培训日期/人数/方式/内容不改；其它文档「记录人」签字格不改 | 无标注人员则下拉为空，不编造 |
| R22 | `dd_004` 数据量/医院家数自动计算 | 打开/换产品覆盖：数据量文档需求←该产品最新 `dd_001` 解析「总计需要采集至少 N 例」（不要测试集 485）写成「总计不少于 N 例数据」；数据量实际情况←最新 `dd_002` 各行回传数据量（空则实际接收量）求和写成「总计约 N 例」；数据来源实际情况←回传医院行数写成「来源于 N 家医院」。检查方式/性别/年龄/地域/设备/协议/参数/精度/是否符合/问题描述/结论/封面/修订/签字不改 | 无 `dd_002` 或回传无医院行则两处实际情况不改；无 `dd_001` 或解析不到例数则文档需求数据量不改 |
| R21 | `dd_003` 采集单位/清洗日期/数据量/清洗人员自动带出 | 打开/换产品覆盖整理表三列←该产品最新 `dd_002`：采集单位←数据来源，脱敏检查、清洗日期←数据接收时间，数据量←回传数据量（空则实际接收量），脱敏检查、清洗人员←参与人员职能「脱敏+清洗人员」轮流。按采集单位与回传医院名匹配，匹配只改这三列 + 清洗人员。回传有、表没有的补一行：所属项目/清洗人员/检查方式/采集设备/采集协议/数据来源场景/评估结论/签字沿用已有最后一条完整行（有采集设备或采集协议或评估结论），数据量仍来自回传，清洗人员有职能则用职能轮流。已有行这几列空则按完整行补全，已有内容不覆盖（清洗人员有职能则覆盖）。表有、回传没有的行去掉。封面/修订不编造 | 无 `dd_002` 或回传无医院行则不改；无完整行则补行这几列仍空 |
| R20 | `dd_002` 医院信息从合规医院列表带出 | 打开/换产品覆盖回传表「数据来源」「医院编号」←该产品合规医院列表（对方单位名称 / 医院编号，按 `sort_order`）。已有行按医院编号优先、名称其次匹配，匹配则保留负责人/数量。清单有、表没有的补一行（数据类型沿用已有首行，其余空）。表有、清单没有的行去掉。「数据回传方式」每行写死「硬盘邮寄」。「数据接收时间」仅填空：时间线「数据部」含「回传」或「多中心」的日期写成 `yyyy.M.d`，多天按早→晚轮流填空行，已有日期不覆盖。「数据采集负责人」←参与人员职能「数据采集人员」轮流，按最新名单覆盖；无该职能则先按 48 默认 6 人（周中亚、李鹏飞、耿景辉、刘新阳、王振宇、王慧阳）写入参与人员再填，写入失败仍用这 6 人填空。封面/修订/签字/数量不编造 | 无清单或无 `prod_hospital_view` 则不改；无匹配时间线则时间仍空；不写回合规列表 |
| R10 | 左侧菜单按归档二级分组，组内按依赖/流水；标注规则/标记规则/标注需求挂数据文件 | 规范（DD-001、MD-003、MD-002、DD-006、DD-007）、采集记录（DD-002～004）、人员记录（DD-005、DD-013）、入库记录（DD-008～009、DD-011、DD-010、DD-012）、数据资产（DD-014、DD-015）、设备与环境维护（清单、DD-016、DD-017） | 不改类型名；`md_002_*`/`md_003` 不出现在模型文件菜单 |
| R11 | `dd_016` / `dd_017` 周检与模型文件说明一致 | 菜单只留说明。编辑页无左侧章节，单页：资产表 + 只读说明 + 各资产检查表（样式对齐 `md_019`）。定期检查后有维护记录章。资产编码 ← 该产品最新 `dd_eq` 组装机/Apple（跳过显示器；`dd_016` 用途含「开发」，`dd_017` 用途含「标注」）。用途含「共用」=服务器，否则开发机/标注机。周日期只取时间线「数据部」列：`dd_016` ← 含「清洗 / 整理 / 回传 / 多中心 / 原始数据库 / 基础数据库 / 数据质量评估」的最早到最晚；`dd_017` ← 含「标注环境维护 / 人员培训 / 培训记录 / 试标注 / 数据标注记录 / 标注数据库 / 标注需求反馈」的最早到最晚；再拆周一～周五。不含型检、设计转换、代码审查、模型构建。检查人 ← 「数据部负责人」否则「数据负责人」否则角色含「数据」（签字图否则姓名）。默认勾选：更新升级/日志是否错误=否，其余=是 | 打开详情/换产品/导出覆盖资产编码（同编码保留设备信息）及周次与检查人；无清单不改资产表；无数据部对应输出则提示暂无周记录 |
| R12 | 记录 / 反馈 / 统计表 / 设备清单单页原表 | `dd_002`～`dd_004`、`dd_005_*`、`dd_008_*`、`dd_009_*`、`dd_010`～`dd_012`、`dd_013_*`、`dd_014`、`dd_015_*`、`dd_eq`。编辑页无左侧章节，只展示原表（跳过封面/修订/产品信息）；标题/文件编号通栏合并居中，数据格居中。`dd_002` / `dd_008_*` / `dd_009_*` / `dd_010` / `dd_eq` / `dd_013_05`～`07` 不做空格纵向/横向合并。培训/考核「参与培训人员名单、审核人员、考核通过人员、参与考核人员」及「检查人员」行姓名格空格不横向合并（避免最后一人名撑格）；左侧标签列按续行纵向合并。`dd_010` 编辑页加宽可横滑，存储路径左对齐换行，其余格仍居中。旧章节树打开时抽出记录表。导出一张 xlsx 对齐原表（标题通栏合并居中），不导出封面/修订。`dd_015_*` 仍可导入统计 Excel 只替换记录表 | 打开即见原表；导出不含封面/修订 |
| R13 | `dd_001` 封面编制人/审核人 | 编制人←模型负责人否则模型部负责人否则角色含「模型」；审核人←角色含「模型」（签名图否则姓名）；批准人仍←研发负责人 | 新增/打开空格/换产品/导出按此带出；其它数据文件仍按第 4 节默认 |
| R14 | 需求/规则/方法 Word 封面编制人/审核/批准 | `md_002_01` / `md_002_02` / `md_003` / `dd_006` / `dd_007`：与 R13 相同。编制人←模型负责人否则模型部负责人否则角色含「模型」；审核人←角色含「模型」；批准人←研发负责人 | 新增/打开空格/换产品/导出按此带出 |
| R16 | `dd_006`「评估流程」2.1～2.5 是正文 | 源 Word 为 Normal，不是 Heading。模板写入「评估流程」正文；打开已存文档时若子章正文/表都空则折回正文（已手写不改） | 目录不再出现 2.1～2.5 子节 |

修订首行、文件编号规则同 99（关键字取该类型名称）。无独立「产品信息」章（打开/新增/导出去掉）。原 Word 有「附件 1 评审记录」表的类型（`dd_001`、`md_003`），附件为空则按原表补上；误挂在其它章节的评审表挪回附件。打开 / 换产品 / 导出时评审时间←时间线该类型关键字（优先含「评审」，`yyyy.MM.dd`），参评人员按表内部门从参与人员带出（规则同 99 附件评审记录），批准人←研发负责人+评审时间。打开或换产品时，章节标题 / 正文 / 表格对产品名称、型号、产品代码做精确全文替换（模板基准名「肺栓塞CT图像辅助评估软件」、型号「IR-CT-PE」换成当前产品；换产品时再把原产品字段整段换成新产品）。签名图不替换。不按疾病词或模块名做模糊匹配。**切换产品时覆盖封面编制人/审核人/批准人及日期、生效日期，以及文件修订记录首行修改日期/修订人/批准人**（编制人←数据部负责人否则数据负责人否则角色含「数据」；审核人←角色含「模型」；批准人/修订批准人←研发负责人；封面日期为 `yyyy.MM.dd`；修订日期为 `YYYY年M月D日`）。**`dd_001` 数据采集需求例外**：编制人←模型负责人否则模型部负责人否则角色含「模型」；审核人←角色含「模型」（有签名图用图，无图用姓名）；批准人/修订批准人仍←研发负责人；修订人跟编制人。**`md_002_01` / `md_002_02` / `md_003` / `dd_006` / `dd_007` 与 `dd_001` 相同**：编制人←模型负责人否则模型部负责人否则角色含「模型」；审核人←角色含「模型」；批准人/修订批准人←研发负责人；修订人跟编制人。编制部门、文件版本、修订说明不因换产品改写。

---

# 5. 数据结构说明

表 `data_doc` 字段同 `model_doc`，见 [01_数据库表结构.md](./01_数据库表结构.md)。`content` 目录树同 99。`format` 记在模板 `DOC_META`，不单独建列。

---

# 6. 异常场景 & 兜底逻辑

与 99 相同（版本重复、详情不存在、类型非法、自动获取源空、保存导出失败、无权限）。大表模板截断不影响已保存的用户数据。

---

# 7. 功能约束 & 修改红线

## 7.1 允许修改范围

- 本文与 `01` 的 `data_doc` 表；`data_doc` 模型/对象/服务/接口/模板；`scripts/data_stats/`；`serv_review_util` 的 `dd_*` 登记；前端 `DataDocs*`、`DataRecordDocDetail.tsx`、`DataEnvDocDetail.tsx`、`DataDocPage.tsx`、`ApiDataDoc.ts`、`Home.tsx` 数据菜单、`index.tsx` 路由。数据统计独立页见 101。

## 7.2 绝对禁止改动（红线）

- 禁止改产品文件封面与模型文件已上线 Word 的导出结构。
- 禁止回写产品主数据；禁止改 `src/store/`、`src/common/`、`src/utils/base/`。
- 必须单表 `data_doc` + `doc_type`。
- 禁止自动获取覆盖已填。

## 7.3 禁止新增逻辑

- 不得新增本文未列出的 `doc_type`。
- 文档集成导出收录不在本文范围。

## 7.4 禁止重构结构

- 不得整文件重写立项报告、不得为赶工改模型 Word 已测接口签名（仅允许给导出增加 xlsx 分支）。

---

# 8. 验收标准

- [ ] 数据文件按归档二级分组（规范、采集记录、人员记录、入库记录、数据资产、设备与环境维护）；评估/考核方法在规范，不在人员记录；数据标注规则 / 标记规则 / 数据标注需求在数据文件下，不在模型文件下。
- [ ] 设备与环境维护菜单为：数据设备清单、开发环境维护记录说明、标注环境维护记录说明（无独立维护记录）。
- [ ] 数据文件分组下各 Word/Excel 子菜单可进列表。
- [ ] 新增后进入编辑页：一般 Word 目录树为该类型模板（含封面、修订记录及样例章节；无产品信息章）；记录 / 反馈 / 统计表为单页原表（无左侧章节）；`dd_016` / `dd_017` 为 DEM 式单页（无左侧章节）。
- [ ] 封面空的编制部门为「数据部」；模型文件仍为「模型部」。
- [ ] 仅填空自动获取；不回写产品。
- [ ] 编辑页切换产品后：封面编制人/审核人/批准人及日期、生效日期、修订记录首行日期/修订人/批准人换成新产品；正文/标题/表格中原产品名称与型号整段换成新产品。`dd_001` / `md_002_01` / `md_002_02` / `md_003` / `dd_006` / `dd_007` 编制人为模型负责人、审核人为角色含「模型」、批准人为研发负责人。
- [ ] Word 导出 docx；记录 / 统计表导出一张 xlsx（对齐原表，不含封面/修订）；其余 Excel 导出 xlsx。
- [ ] `dd_001` / `md_003` 等首格含「评审记录」的表：编辑页按 Word 合并格展示（相邻相同非空格横向合并，首列纵向合并），不再把合并格拆成重复列。
- [ ] `dd_016` / `dd_017` 打开/换产品/导出：编辑页无左侧章节，单页资产表+只读说明+周检表（对齐 `md_019`）；资产编码取自该产品最新数据设备清单（组装机或 Apple，跳过显示器，按用途开发/标注筛选）；周检日期取数据部时间线（开发=清洗/整理/回传/上传统计，标注=维护说明/培训/试标/标注）；检查人为数据部负责人否则数据负责人否则角色含「数据」；菜单无独立维护记录；导出一份 docx。
- [ ] 复制递增版本；删除生效。
- [ ] 无 `data_doc_view` 看不到菜单。
- [ ] `dd_015_*` 新增为空表头；数据统计选产品并算完后写入该产品最新对应统计表记录表（数据分布 + `case_rows`）；无该文档则提示先新增；仍可导入统计 Excel；封面/修订不被覆盖；编辑页仍只见数据分布原表。
- [ ] 数据统计选产品并算完后，按 R18 写入试标注/标注记录（含日期、标注人员、项目、类型、状态、审核结果；审核医生有职能才填）；无该文档不建档；封面/修订不被覆盖。
- [ ] 打开/换产品 `dd_011`：文档需求数据量与交付时间来自最新 `md_003`，实际情况来自最新 `dd_010`（无则 `dd_012`），`44943` 转为 `2023.1.17`；封面/修订不改；无源不编造。
- [ ] 打开/换产品/导出 `md_003`：两段「数据」训练/测试/调优数据量来自最新 `dd_010`（无则 `dd_012`）同名数据集，写成「N例左右」且保留「左右」；无对应数据集则该行不改；交付时间/任务描述/封面/修订不改。
- [ ] 数据 Word（`md_002_*` / `md_003` / `dd_001` / `dd_006` / `dd_007`）编辑/导出正文、图、图题、表格交错；`md_002_01` 标注示例图、`md_002_02` 标记示例图能显示；已有 `images` 不覆盖。
- [ ] 打开/换产品 `dd_002`：数据来源/医院编号来自该产品合规医院列表；回传方式每行「硬盘邮寄」；空的接收时间来自时间线数据部回传/多中心日期；数据采集负责人来自参与人员「数据采集人员」轮流并按最新名单覆盖（无则先写入默认 6 人）；匹配行已有负责人/数量保留；清单外样例行去掉；无清单不改；封面/修订不改。
- [ ] 打开/换产品 `dd_003`：采集单位/清洗日期/数据量来自该产品最新 `dd_002`，清洗人员来自参与人员职能「脱敏+清洗人员」轮流；匹配行只改这三列 + 清洗人员；补行及空的设备/协议/场景/结论/签字沿用最后一条完整行；回传外样例行去掉；无回传不改；封面/修订不改。
- [ ] 打开/换产品 `dd_004`：数据量文档需求来自最新 `dd_001`「总计需要采集至少 N 例」（7000，不是测试集 485）；数据量实际情况为最新 `dd_002` 回传量合计；数据来源实际情况为回传医院家数；其余要点/是否符合/结论不改；无源不改；封面/修订不改。
- [ ] `dd_005_01` / `dd_005_02` 讲解人员、记录人为标注人员下拉，单击选中即保存；无标注人员则空；其它培训格仍手写。
- [ ] 打开/换产品 `dd_005_*`：参与培训/考核通过名单来自标注人员，审核人员来自审核医生；无该块则补上；无对应职能则空；内容/结果不改。
- [ ] 打开/换产品 `dd_014`：检查日期来自时间线数据部「数据库维护」；一张表一天，样例表按天数增减；无匹配日不改；检查内容/人员不改。
- [ ] 打开/换产品 `dd_013_01`～`04`：参与考核人员来自标注人员，人数同步；考核时间来自时间线数据部考核或培训/培训记录（按肺叶/肺栓塞；初次最早一天，定期多天换行）；无匹配日不改。`dd_013_05`～`07` 日常表按标注人员一人一行、审核医生轮流，空打分按模板随机（每行一个 B），空日期按时间线标注记录（无则考核/培训）轮流填写为 `yyyy-MM-dd`、右列加一天，已填日期只统一格式；姓名/审核医生可下拉保存，日期可日历选择保存；要点/细则不改。
- [ ] 记录 / 反馈 / 统计表 / 设备清单（`dd_002`～`dd_004`、`dd_005_*`、`dd_008_*`、`dd_009_*`、`dd_010`～`dd_014`、`dd_013_*`、`dd_015_*`、`dd_eq`）编辑页无左侧章节、单页原表（标题/编号通栏居中）；导出一张 xlsx 对齐原表，不含封面/修订。
- [ ] `dd_006`「评估流程」为一段 2.1～2.5 正文，目录无 2.1～2.5 子节；评估规则下三个子节保留。
