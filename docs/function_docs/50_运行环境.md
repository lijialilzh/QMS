# 运行环境

| 项 | 内容 |
|----|------|
| 功能编号 | 50 |
| 所属模块 | 产品管理 |
| 主要代码位置 | 后端 `model/prod_runtime_env.py`、`serv/serv_prod_runtime_env.py`、`api/api_prod_runtime_env.py`；前端 `pages/syscfg/ProdRuntimeEnv.tsx`、`ProdRuntimeEnv.less`、`api/ApiProdRuntimeEnv.ts` |
| 文档版本 | v1.0 |
| 最后更新 | 2026-06-24 |

---

## 1. 功能概述

按**产品（版本）**维护《2.4 运行环境》标准模板：固定结构的 4 张表，作为几乎所有交付文档都会引用的「运行环境」章节来源。每个产品一条记录，字段固定、预填模板默认值，可逐项修改。

- 数据维度：产品版本级（`prod_id`，与产品参与人员/时间线一致），一个产品唯一一条记录（`prod_id` 唯一）。
- 模板结构：架构说明（B/S）+ 表1 服务器硬件 + 表2 服务器软件 + 表3 用户端 + 表4 网络要求。

---

## 2. 前置依赖 & 关联模块

- 依赖：产品主数据（`/product/list_product`）提供「产品名称 + 完整版本」级联下拉（`ProductVersionSelect`）。
- 页面：`pages/syscfg/ProdRuntimeEnv.tsx`（自绘 HTML 表格还原模板版式）；样式 `ProdRuntimeEnv.less`；API `api/ApiProdRuntimeEnv.ts`。
- 菜单：「产品管理 → 运行环境」`/prod_runtime_env`，权限 `prod_runtime_view`。
- 【只读 / 禁止修改】：`/product/*` 产品模块。

---

## 3. 完整执行流程（分步细化）

1. 进入页面 → 拉取产品下拉（`list_product`），用产品名称 + 完整版本级联选择。
2. 选择产品（版本）→ `get_prod_runtime_env(prod_id)`：
   - 已有记录：返回该产品已保存的运行环境；
   - 未建记录：返回**模板默认值**（带 `prod_id`，不落库），供前端预填。
3. 编辑（点击字段直接编辑、失焦实时保存）：
   - 每个值单元格为多行文本（`Input.TextArea`，自动撑高）；
   - 失焦时若该字段较上次保存快照有变化 → 调 `save_prod_runtime_env`（按 `prod_id` upsert，首次保存才创建记录）；无变化不发请求；
   - 保存成功后更新本地快照并 `message` 提示成功。

---

## 4. 核心规则 & 判定条件

- `get_prod_runtime_env`：`prod_id` 必填；无记录时回落模板默认值。
- `save_prod_runtime_env`：`prod_id` 必填，按 `prod_id` upsert（存在则更新、不存在则新建）；仅写入可编辑字段，不接受 `id` 覆盖。
- 一个产品唯一一条记录（`prod_id` 列 `UNIQUE`）。
- 权限：写操作 `prod_runtime_edit`，读操作 `prod_runtime_view`；默认与「产品版本管理」权限同步（有 `product_view` 者可查看、有 `product_edit` 者可编辑）。

---

## 5. 数据结构说明

表 `prod_runtime_env`（详见 [01_数据库表结构.md](./01_数据库表结构.md)）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | 主键 |
| prod_id | Integer NOT NULL, UNIQUE, index | 产品 ID（产品版本），一对一 |
| arch | String(256) | 架构说明（默认「软件为B/S架构」） |
| srv_cpu / srv_memory / srv_gpu / srv_disk / srv_nic | TEXT | 表1 服务器硬件（多行） |
| srv_os / srv_cuda | TEXT | 表2 服务器软件 |
| cli_cpu / cli_memory / cli_resolution / cli_os / cli_browser | TEXT | 表3 用户端 |
| net_lan / net_wan | TEXT | 表4 网络（局域网/广域网带宽） |

模板默认值（后端常量 `DEFAULT_RUNTIME_ENV`，与标准模板一致）：
- 服务器 CPU：主频至少 2GHz / 核心数 10 核及以上 / x86 指令集；内存 ≥64G；GPU 英伟达、显存 ≥8GB、FP16 14TFLOPS；硬盘 系统盘 ≥500GB、存储盘 ≥3TB；网卡 千兆 PCI-E（支持 Linux）。
- 服务器软件：Ubuntu 24.04LTS（64位）/ CUDA 12.6。
- 用户端：CPU 酷睿 i5 及以上 / 内存 ≥16GB / 分辨率 1920*1080 / Windows 10 专业版（64位）及兼容版本 / Chrome 137.0 及兼容版本。
- 网络：局域网 100Mbps 及以上 / 广域网 1000Mbps 及以上。

---

## 6. 异常场景 & 兜底逻辑

- 未选产品：表格输入禁用并提示「请先选择产品」，不发请求。
- 失焦无改动：不触发保存。
- 保存失败：`message.error` 提示后端 msg，本地快照不更新。
- 数据库异常：后端 `rollback` 并返回 `msg_err_db`。

---

## 7. 功能约束 & 修改红线

1. 模板结构固定（4 张表、字段名固定），不在此模块做动态增删表/行。
2. `save` 不接受修改 `id`、不允许跨产品迁移（按 `prod_id` upsert）。
3. 默认值仅用于「未建记录」预填，不强制覆盖已保存数据。
4. 一个产品仅一条记录（`prod_id` 唯一）。

---

## 8. 验收标准

- [ ] 选产品（版本）后展示《2.4 运行环境》4 张表，未建记录时显示模板默认值。
- [ ] 修改任一字段失焦后实时保存成功并提示。
- [ ] 切换产品后展示各自独立的数据，互不影响。
- [ ] 无 `prod_runtime_view` 权限者看不到菜单、调用接口被拦截。
