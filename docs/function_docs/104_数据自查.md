# 数据自查 功能逻辑 & 开发文档

> 文档元信息（每次修改必须更新）
>
> | 项 | 内容 |
> |----|------|
> | 功能编号 | 104 |
> | 所属模块 | 数据质控 / 数据自查 |
> | 主要代码位置 | 前端 `pages/data_check/DataCheck.tsx`；菜单 `pages/Home.tsx`、路由 `index.tsx`；i18n `menu.data_check`；复用 `pages/data_stats/dataStatsLocal.ts`、`api/ApiDataDoc.ts` |
> | 文档版本 | v1.2 |
> | 最后更新 | 2026-09-20 |

---

# 1. 功能概述

- **用途**：给模型人员、数据人员做数据对账。按已设定亚组核对灵敏度/特异度是否达到目标值；核对三库例数、回传/整理/上传/查重是否对齐，以及 TXID、病例标识和性别/年龄/设备是否完整。不满足时指出哪一条。
- **使用场景**：左侧菜单「数据质控 → 数据自查」`/data_check`。
- **最终目标**：只读该产品已有数据统计缓存 / `dd_015_*`、多中心回传 `dd_002`、数据整理 `dd_003`、上传记录 `dd_010`、查重记录 `dd_012`，不改这些文档、不新建表。
- **关联**：亚组口径对齐 [101_数据统计.md](./101_数据统计.md) 统计结果（因素下的类别即亚组）。回传/整理表结构见 [100_数据文件管理.md](./100_数据文件管理.md)。

---

# 2. 前置依赖 & 关联模块

## 2.1 涉及页面

| 页面 / 路由 | 作用 |
|------------|------|
| `/data_check` | 数据自查 |

## 2.2 涉及组件

| 组件 / 文件 | 作用 |
|------------|------|
| `pages/data_check/DataCheck.tsx` | 页面：选产品、目标值、剔除例数、自查结果 |
| `pages/data_stats/dataStatsLocal.ts` | 读本机统计缓存、从 `dd_015` 抽 `case_rows`、生成亚组四格表 |

## 2.3 涉及接口

权限：进菜单 `data_doc_view`。只读已有接口，不新增后端。

| M | 路径 | 权限 | 入参 | 出参 |
|---|------|------|------|------|
| GET | `/trace-api/data_doc/list_data_doc` | `data_doc_view` | `{ product_id, doc_type, page_index:0, page_size:1 }` | 该产品最新一份该类型文档 |
| GET | `/trace-api/data_doc/get_data_doc` | `data_doc_view` | `{ id }` | 文档 `content` |

## 2.4 【只读 / 禁止修改】模块清单

| 模块 / 文件 / 函数 | 标记 | 原因 |
|--------------------|------|------|
| `src/store/`、`src/common/`、`src/utils/base/` | 【禁止修改】 | 全局公共 |
| `dd_002` / `dd_003` / `dd_015_*` 表结构 | 【只读】 | 自查不改这些文档 |

---

# 3. 完整执行流程（分步细化）

## 3.0 触发入口

菜单「数据质控 → 数据自查」进入 `/data_check`，与「数据统计」同组。无 `data_doc_view` 不显示。

## 3.1 选择产品与参数

1. 选择产品（名称+完整版本）。未选不能自查。
2. 目标值默认 `0.8`，可改。灵敏度和特异度都按该值比较。
3. 剔除例数可手填；不填则从最新 `dd_003` 各行「评估结论」解析「N例」后求和。

## 3.2 点「自查」

### 3.2.1 亚组

1. 病例明细优先本机缓存（标注→基础→原始），没有则从该产品最新 `dd_015_03` / `_02` / `_01` 的 `case_rows` 读。
2. 按 101 统计结果同一套因素生成亚组（原「类别」在本页叫「亚组」）。
3. 灵敏度、特异度为 `/` 或空则跳过该项。
4. 数值 `< 目标值` 记为不满足，列出因素、亚组、指标、实际值。
5. 能比较的项全部 ≥ 目标则该项通过。无明细则提示先做数据统计。

### 3.2.2 多中心回传总数

1. 原始库总数 ← 最新 `dd_015_01` 的 `case_rows` 行数；没有则读表中「数据总量（序列）」。
2. 回传合计 ← 最新 `dd_002` 各医院行「回传数据量」（空则「实际接收量」）求和；跳过记录人/复核人行。
3. 两数不等则提示回传合计与原始库总数。缺文件则提示缺哪份。

### 3.2.3 整理记录与基础库

1. 数据量口径：原始库总数（同上）。
2. 基础库期望值 = 原始库总数 − 剔除例数。
3. 基础库实际值 ← 最新 `dd_015_02` 行数或「数据总量（序列）」。
4. 不等则提示期望值与实际值。`dd_003` 没有且未手填剔除时，剔除按 0。

### 3.2.4 三库例数

原始 / 基础 / 标注总数分别来自 `dd_015_01` / `_02` / `_03`。三者相等，或满足「原始 ≥ 基础 ≥ 标注」为通过；缺表或基础大于原始、标注大于基础则不通过。

### 3.2.5 回传与实际接收

最新 `dd_002` 各医院行「回传数据量」应等于「实际接收量」。列出不一致的家数和首家医院。

### 3.2.6 回传医院与整理医院

`dd_002`「数据来源」集合应等于 `dd_003`「采集单位」集合。只在一边出现的医院计入失败。

### 3.2.7 整理数据量与回传

`dd_003` 各行「数据量」合计应等于 `dd_002` 回传合计（空则实际接收量）。

### 3.2.8 上传记录与统计表

最新 `dd_010`：原始/基础表医院行「上传数据量」合计对照对应 `dd_015_*` 例数；标注表若各数据集数量相同则取该值，否则求和，对照 `dd_015_03`。

### 3.2.9 查重与标注上传

最新 `dd_012` 各「批次」数据量按名称对照 `dd_010` 标注库「数据集」上传数据量。缺名或数量不等则不通过。

### 3.2.10 病例标识 / TXID 与回传医院 / 关键字段

病例明细优先本机缓存再 `dd_015_*`。TXID 必须有且不重复；TXID 第一段（`-` 前）应能命中 `dd_002` 医院编号；性别 / 年龄 / 设备应都有值。PatientID 空只提示，不单独判失败。

## 3.3 展示

本页列出通过/不通过条目，不写回任何数据文件。

---

# 4. 核心规则 & 判定条件

| 编号 | 规则 / 判定条件 | 含义 | 不满足 / 命中时的后果 |
|------|----------------|------|----------------------|
| R1 | 权限 `data_doc_view` | 与数据统计同权 | 菜单不可见 |
| R2 | 只读、不落库 | 不改 `dd_002` / `dd_003` / `dd_010` / `dd_012` / `dd_015_*` | 自查结果仅本页展示 |
| R3 | 亚组 = 统计结果的类别 | 因素下每一类为一亚组 | 口径与 101 一致 |
| R4 | 目标值默认 0.8 | 灵敏度、特异度均与该值比 | 低于目标列出该亚组该指标 |
| R5 | `/` 不算不满足 | 无法计算则跳过 | 不列入失败 |
| R6 | 回传合计 vs 原始库总数 | 多中心各行数量求和 | 不等则提示两条数字 |
| R7 | 基础库 = 原始库 − 剔除 | 评估结论「N例」或手填 | 与 `dd_015_02` 不等则提示 |
| R8 | 三库例数 | 相等或 原始≥基础≥标注 | 缺表或倒挂则提示三数 |
| R9 | 回传数据量 = 实际接收量 | 按医院行比 | 不等列出家数 |
| R10 | 回传医院 = 整理医院 | 数据来源 vs 采集单位 | 只在一边的计入失败 |
| R11 | 整理数据量合计 = 回传合计 | 空回传量用实际接收量 | 不等提示两数 |
| R12 | 上传记录对照统计表 | 原始/基础医院行合计；标注同量取该值否则求和 | 与对应 `dd_015_*` 不等则提示 |
| R13 | 查重批次对照标注上传 | 按数据集名比数据量 | 缺名或不等则提示 |
| R14 | TXID 完整且命中回传医院；性别/年龄/设备有值 | TXID 第一段 = 医院编号 | 空、重复或未命中则提示 |

---

# 5. 数据结构说明

无新表。读取已有 `data_doc` 的 `content` 与本机 `qms_data_stats_v1` 缓存。额外只读 `dd_010` / `dd_012`。

---

# 6. 异常场景 & 兜底逻辑

- 未选产品：提示「请先选择产品」。
- 无病例明细：亚组项提示「请先在数据统计生成或写入病例明细」。
- 无 `dd_002`：回传项提示「没有多中心数据回传记录」。
- 无 `dd_015_01` / `_02`：对应总数项提示缺原始/基础统计表。
- 评估结论解析不到「N例」且未手填：剔除按 0。
- 无 `dd_010` / `dd_012`：对应上传/查重项提示缺文件。
- 无病例明细：病例标识、TXID 对照、关键字段提示先做数据统计。

---

# 7. 功能约束 & 修改红线

## 7.1 允许修改范围

- 本文；前端 `pages/data_check/`；`Home.tsx` 本菜单；`index.tsx` 本路由；i18n `menu.data_check`。只读调用已有 `data_doc` 接口与 `dataStatsLocal`。

## 7.2 绝对禁止改动（红线）

- 禁止改 `src/store/`、`src/common/`、`src/utils/base/`。
- 禁止自查时更新 `dd_002` / `dd_003` / `dd_010` / `dd_012` / `dd_015_*`。
- 禁止经浏览器上传 DICOM。

## 7.3 禁止新增逻辑

- 不得在无 gt/pred 时编造灵敏度、特异度。
- 不得把全库统计当成某个训练集子集去改构建记录。

## 7.4 禁止重构结构

- 不得把本页合并进数据统计页或数据文件编辑页。

---

# 8. 验收标准

- [ ] 左侧「数据质控」下有「数据统计」「数据自查」，`data_doc_view` 可见。
- [ ] 选产品后点自查：亚组低于目标值列出因素/亚组/指标；`/` 不列入失败。
- [ ] `dd_002` 回传合计与 `dd_015_01` 总数不等时提示。
- [ ] 原始库 − 剔除 与 `dd_015_02` 不等时提示。
- [ ] 自查列出三库例数、回传与实际接收、回传/整理医院、整理数据量、上传记录与统计表、查重与标注上传、病例标识、TXID 与回传医院、关键字段完整。
- [ ] 自查不改任何数据文件。
