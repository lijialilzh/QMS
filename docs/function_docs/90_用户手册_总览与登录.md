# 用户操作说明书（一）总览、登录与通用约定

> 本系列（90–98）以**用户实际操作**为基准编写，逐步说明每个功能的操作流程、字段填写、适用范围与边界测试，供使用者与测试人员对照执行。

## 0. 阅读约定
- 「菜单 → 子菜单」表示从左侧导航逐级进入。
- 「点击 X」指页面上的按钮 / 链接 / 图标。
- **必填**字段未填写时无法提交，系统给出红色提示。
- 「边界测试」列出该功能需重点验证的异常 / 极限场景。

---

## 1. 系统简介与适用范围

QMS 是面向**医疗器械软件**的质量管理追溯系统，覆盖：
- 需求规格说明（SRS）→ 软件详细设计（SDS）→ 测试 的全链路追溯；
- 风险管理（危害 HAZ / 控制措施 RCM / 网络安全 CST / 风险管理报告）；
- 系统配置（用户、角色、项目、产品）。

**适用人群**：质量（DQA/QA）、法规（RA）、产品经理、研发、测试等角色。
**适用浏览器**：现代浏览器（Chrome / Edge 等）。

---

## 2. 登录与退出

### 2.1 登录
1. 打开系统地址，进入登录页。
2. 在登录框输入**账号**（输入框带用户图标）与**密码**（带锁图标，输入内容自动隐藏为圆点）；账号或密码留空时点「登录」，会提示“请输入用户名!”或“请输入密码!”。
3. 点击「登录」按钮：
   - 成功：进入系统主界面（默认落地页按当前角色权限展示）。
   - 账号不存在或密码错误：出于安全考虑，统一提示“用户不存在！”（不单独区分是账号还是密码错误）。
4. 登录页下方有“忘记密码?”链接，点击后提示“请联系您的管理员重置密码。”

### 2.2 字段说明
- 账号（必填）：管理员分配的登录名。
- 密码（必填）：新用户初始密码为系统默认值（由环境配置），建议首次登录后立即修改。

### 2.3 退出登录
点击右上角用户名区域 → 在下拉菜单中点「退出登录」→ 弹出确认框“确认退出登录吗？”→ 点「确定」，系统提示“退出登录成功”并返回登录页。

### 2.4 修改密码
1. 点击右上角用户名区域 →「修改密码」，弹出“修改密码”对话框。
2. 依次填写：原密码、新密码、确认新密码（均必填）。
3. 点「确定」提交：
   - 修改成功：提示“请求处理成功!”。
   - 两次新密码不一致：提示“新密码与确认新密码不一致”。
   - 原密码错误：提示“原密码错误”。
   - 新密码为空：提示“请输入新密码!”。

### 2.5 边界测试
| 场景 | 预期 |
|------|------|
| 账号或密码留空提交 | 提示“请输入用户名!”/“请输入密码!” |
| 账号不存在或密码错误 | 统一提示“用户不存在！” |
| 会话超过 24 小时未活动后操作 | 返回登录页，需重新登录 |
| 修改密码时原密码错误 | 提示“原密码错误”，不允许修改 |
| 两次新密码不一致 | 提示“新密码与确认新密码不一致” |

---

## 3. 主界面与导航

左侧为功能菜单（按当前角色权限显示），主要分组：
- **仪表盘**：系统首页。
- **系统配置**：角色管理、用户管理、产品线管理。
- **通用配置**：打印服务配置、版本命名规则、公司基本信息、人员签名管理。
- **基础数据**：HAZ管理、RCM管理、THREAT管理。
- **产品管理**：基本信息（版本管理、参与人员、时间逻辑线、运行环境、设备资源）、风险管理（风险参与人员、产品HAZ管理、产品RCM管理、产品THREAT管理）、产品图示（物理拓扑图、体系结构图、用户界面关系图、网络安全流程图）。
- **产品文件管理**：产品文件（产品立项报告、产品开发计划、需求规格说明、产品验收记录、产品发布说明、版本更新历史、产品技术要求、自研软件研究报告）、风险文件（风险管理计划、初步危害分析清单、自研软件网络安全研究报告、网络安全能力分析、产品标签样稿、网络安全维护计划、风险管理报告）、其他（培训记录表、产品DHF管理）。
- **开发测试文件管理**：开发文件（软件配置管理计划、软件配置状态报告、软件开发计划、软件详细设计、开发环境维护说明、代码审查记录、数据申请单、开发设备清单）、测试文件（软件测试计划、Bug管理及回归测试、软件测试报告、测试环境维护说明、安装维护手册、现场测试规程、现场测试记录、用户测试计划、用户测试报告、测试设备清单、测试用例合集、追溯分析）、网络安全管理（网络安全风险管理报告）。
- **全局视图**：文档内容比对、整合导出（整合导出、导出记录）、一键打印（一键打印、打印记录）。

> 菜单项是否可见取决于角色权限；无权限的菜单不显示，无权限的操作会提示「您没有权限执行此操作」。

---

## 4. 通用操作约定（所有列表页通用）

### 4.1 查询 / 筛选
- 多数列表页顶部提供筛选条件（如产品、版本、关键词）。
- 选择 / 输入条件后点「搜索」（圆形放大镜）从第 1 页重新查询。
- 「选择产品」控件为产品名称 + 完整版本联动；部分页面需先选产品才能加载版本下拉。

### 4.2 新建 / 编辑
- 点「新建 / 添加」打开弹窗或跳转新建页；填写必填字段后提交。
- 点行内「编辑」修改。

### 4.3 删除
- 单条删除：点行内「删除」→ 弹出确认框「确认删除？」→ 确认执行。
- 批量删除：先勾选行 → 点「批量删除」；未勾选时提示「请选择要操作的数据」。
- 删除被引用的数据会被拒绝（见各模块边界）。

### 4.4 导入 / 导出
- **导入**：选择符合模板的 Excel（`.xlsx`）/ Word（`.docx`）文件后上传；格式不符会提示对应错误。
- **导出**：按当前筛选条件导出 Excel / Word 文件（浏览器下载）。

### 4.5 分页
- 列表底部分页，可切换每页条数（常见 20/50/100；部分页为 100/500/1000）。
- 注意：产品 RCM/HAZ/CST、测试用例等列表为**前端分页**（后端一次返回全部匹配）。

### 4.6 通用边界测试
| 场景 | 预期 |
|------|------|
| 必填项留空提交 | 红色提示「请输入 / 请选择 {字段}」 |
| 唯一性冲突（名称/编号重复） | 提示「数据已存在」 |
| 删除被引用对象 | 提示对应「存在关联…」并阻止删除 |
| 无权限点击操作 | 提示「您没有权限执行此操作」 |
| 网络/服务异常 | 提示「数据库处理异常，请稍后再试」或对应错误 |

---

## 5. 角色与权限速查

系统预置 7 个固定角色：root（超级管理员）、dqa、qa、ra、产品经理、开发、测试；固定角色不可删除、不可改名。

- 产品经理：只能查看 / 修改自己创建或被分配为成员的产品及其下数据。
- 其它非管理员用户：产品相关列表按“产品成员关联”或“本人创建”过滤。
- 超级管理员 root：可见系统全部数据。

---

## 6. 推荐操作主线（端到端）

1. **系统配置**：建项目 → 建产品 → 配角色与用户 → 配公司信息/人员签名/版本命名规则/打印服务（手册二）。
2. **需求**：建 / 导入 SRS 文档 → 维护需求条目、变更需求表、功能描述（手册三）。
3. **设计**：基于 SRS 建 / 导入 SDS 文档 → 同步追溯 → 维护设计条目（手册四）。
4. **风险**：维护风险基础库 → 关联到产品 → 编写风险管理报告 / 风险管理计划 / PHA / 网络安全能力分析 / NSR / NSMP / 标签样稿（手册五）。
5. **产品文件**：编写产品验收记录 / 自研软件研究报告 / 漏洞文档 / 产品发布说明 / 产品DHF（手册七）。
6. **开发文件**：编写软件配置管理计划 / 软件开发计划 / 开发环境维护说明 / 代码审查记录 / 数据申请单 / 开发设备清单（手册八）。
7. **测试**：导入测试集与用例 → 编写软件测试计划 / 软件测试报告 / 缺陷文档 / 安装维护手册 / 现场测试记录 / 用户测试计划与报告 / 培训记录（手册六）。
8. **追溯核对**：追溯总览、文档对比、整合导出、一键打印（手册九）。
