# 数据统计 功能逻辑 & 开发文档

> 文档元信息（每次修改必须更新）
>
> | 项 | 内容 |
> |----|------|
> | 功能编号 | 101 |
> | 所属模块 | 数据统计 |
> | 主要代码位置 | 前端 `pages/data_stats/DataStats.tsx`、`pages/data_stats/dataStatsLocal.ts`、`pages/data_stats/DataStats.less`；菜单 `pages/Home.tsx`、路由 `index.tsx`；备用脚本 `trace-api/scripts/data_stats/` |
> | 文档版本 | v1.2 |
> | 最后更新 | 2026-08-25 |

---

# 1. 功能概述

- **用途**：在浏览器内从本机病例 DICOM 文件夹生成「因素 / 类别 / 序列数 / 占比」统计表并展示，可下载 Excel。
- **使用场景**：左侧菜单「数据统计」；面向不跑服务器脚本的用户。
- **最终目标**：全产品通用，不写死路径；DICOM 不上传服务器；不落库。
- **归档**：需要写入数据文件 `dd_015_01` / `dd_015_02` / `dd_015_03` 时，下载 Excel 后到对应编辑页「导入统计 Excel」，规则见 [100_数据文件管理.md](./100_数据文件管理.md)。
- **关联**：统计口径对齐备用脚本 `summarize_db_stats.py`（年龄分段 `0,18,40,60,110`；每病例文件夹读 1 个 DICOM 头）。封面/产品信息自动填不在本模块。

---

# 2. 前置依赖 & 关联模块

## 2.1 涉及页面

| 页面 / 路由 | 作用 |
|------------|------|
| `/data_stats` | 数据统计（选文件夹、展示、下载） |

## 2.2 涉及组件

| 组件 / 文件 | 作用 |
|------------|------|
| `pages/data_stats/DataStats.tsx` | 页面 |
| `pages/data_stats/dataStatsLocal.ts` | 本地读 DICOM 头并汇总 |
| `pages/data_stats/DataStats.less` | 样式 |

## 2.3 涉及接口

无新增后端接口。权限沿用 `data_doc_view`（能看数据文件即可进本菜单）。

## 2.4 【只读 / 禁止修改】模块清单

| 模块 / 文件 / 函数 | 标记 | 原因 |
|--------------------|------|------|
| `src/store/`、`src/common/`、`src/utils/base/` | 【禁止修改】 | 全局公共 |
| 产品文件、模型文件已上线 Word | 【禁止修改】 | 本功能只加统计页 |
| `data_doc` 表结构与 `dd_*` 类型清单 | 【只读】 | 归档仍走 100 |

---

# 3. 完整执行流程（分步细化）

## 3.0 触发入口

菜单「数据统计」进入 `/data_stats`。无 `data_doc_view` 不显示。

## 3.1 选择报表类型

单选：原始数据库 / 基础数据库 / 标注数据库。仅影响表头标题与下载文件名。

## 3.2 选择病例文件夹

用户用文件夹选择器选出本机根目录。约定：每个病例一个子文件夹（其下为 DICOM）；根下直接是病例，或「中心/批次 → 病例」均可。浏览器只读每个病例夹内第一个可解析 DICOM 的文件头（最多读前 256KB），文件不上传。

请用 Chrome / Edge。

## 3.3 展示

页面用页签展示：病例明细、数据分布、统计结果。统计人、数据类型、疾病构成可在页上填写后反映到「数据分布」表头。

## 3.4 下载

统计成功后自动下载一份 xlsx（浏览器「下载」文件夹），工作表与离线脚本对齐，不少表：

| 工作表 | 对齐脚本 | 内容 |
|--------|----------|------|
| 病例明细 | `data_statistics_multi.py` | 每病例一行，DICOM 头字段（PatientID/SEX/AGE/DEVICE/KVP/层厚/卷积核等） |
| 数据分布 | `summarize_db_stats.py` | 因素/类别/序列数/占比，供 `dd_015_*` 导入 |
| 统计结果 | `triage_subgroup.py`（无 gt/pred 分支） | SEX/DEVICE/KVP/THICKNESS/ConvolutionKernel 计数 |
| 设备分布 | 同上 | 统计结果的 Item/Catgory/pos_cases/neg_cases |

无标注列时 Sen/Spe 留空；Dice 需表中有 gt/pred/dice，本页从 DICOM 读不到则不出该表。不写到病例目录。也可再点「下载 Excel」。不落库。

---

# 4. 核心规则 & 判定条件

| 编号 | 规则 / 判定条件 | 含义 | 不满足 / 命中时的后果 |
|------|----------------|------|----------------------|
| R1 | 权限 `data_doc_view` | 与数据文件同权 | 菜单不可见 |
| R2 | 文件留在本机 | 不上传 DICOM | 无后端存储 |
| R3 | 一病例一文件夹，只读 1 个 DICOM 头 | 与离线脚本一致 | 无子文件夹则整目录算 1 例 |
| R4 | 年龄分段 0–18–40–60–110 | 与 `summarize_db_stats.py` 一致 | 无年龄则该因素无行 |
| R5 | 性别 / 设备 / KVP / 层厚 / 卷积核按实际值分组 | 缺列不编造 | 全空则该因素不出现 |
| R6 | 不写 `data_doc` | 本页只展示下载 | 归档须走 100 导入 |
| R7 | 不改封面自动填 | 与 99/100 分离 | 本页无封面 |
| R8 | 下载含病例明细+数据分布+统计结果+设备分布 | 对齐离线脚本 | 不得只导出数据分布一张表 |

---

# 5. 数据结构说明

无新表。页面内存：每病例 DICOM 头一行。下载四张工作表（病例明细 / 数据分布 / 统计结果 / 设备分布），不入库。`dd_015_*` 导入时用「数据分布」表。

---

# 6. 异常场景 & 兜底逻辑

- 未选到文件夹或 0 个病例：提示「未找到病例文件夹」。
- DICOM 头解析失败：跳过该病例；全部失败则提示「无法读取 DICOM」。
- 非 Chrome/Edge 可能无法选文件夹：页面注明浏览器要求。
- 隐藏文件（`.` 开头路径段）忽略。

---

# 7. 功能约束 & 修改红线

## 7.1 允许修改范围

- 本文；前端 `pages/data_stats/`；`Home.tsx` 本菜单项；`index.tsx` 本路由；i18n `menu.data_stats`。备用脚本只读对齐口径。

## 7.2 绝对禁止改动（红线）

- 禁止改产品文件 / 模型文件 Word、禁止回写产品主数据。
- 禁止改 `src/store/`、`src/common/`、`src/utils/base/`。
- 禁止把 DICOM 传到服务器；禁止为本功能新建数据库表。
- 禁止改 `dd_015_*` 封面编制部门规则。

## 7.3 禁止新增逻辑

- 不得在无 gt/pred 时编造灵敏度、特异度、Dice（与 `triage_subgroup.py` 无标注分支一致，只出计数）。
- 不得自动写入 `data_doc`。

## 7.4 禁止重构结构

- 不得把本页合并进数据文件编辑页。

---

# 8. 验收标准

- [ ] 左侧有独立菜单「数据统计」，有 `data_doc_view` 可见。
- [ ] 选择本机病例文件夹后页面展示因素/类别/序列数/占比。
- [ ] DICOM 不出现在网络请求体中。
- [ ] 可下载 Excel；模型文件与数据文件其它菜单行为不变。
- [ ] 下载的 Excel 含病例明细、数据分布、统计结果、设备分布；数据分布可在 `dd_015_*` 导入且不覆盖封面/修订/产品信息。
