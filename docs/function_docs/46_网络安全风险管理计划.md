# 网络安全风险管理计划（Cybersec Plan）

## 1. 功能概述

按产品维护「网络安全风险管理计划」文档（章节树 JSON），属于测试文件。与「网络安全风险管理报告」（47）配套——本模块是计划阶段文档，47 是执行结果文档。支持导出 docx。

> 与「风险管理计划」（64）独立并行——本模块面向 IEC 62304/IEC 81001-5-1 / FDA 网络安全要求，不复用 rmp_doc 模型与表，零耦合。

---

## 2. 用户操作流程

### 2.1 列表

按产品/版本筛选 → 新建空白 / 复制版本（弹窗选目标产品，支持跨产品复制） / 编辑文件编号 / 导出 docx / 删除。

列表列：产品名称、产品版本、文档版本、文件编号（单击编辑）、变更说明、创建时间、操作（查看/编辑/复制/导出/删除）。

### 2.2 详情（`/cybersec_plan_docs/add | edit/:id | view/:id`）

章节树编辑、`content` JSON、产品信息自动获取（名称/版本/项目时间/人员/日期）、导出 Word。

**新增模式选产品后自动联动**：
- 选定产品后自动拉取产品信息，填充封面/修订记录/正文章节。
- 自动填充文本章节（**仅当章节正文为空时填**，不覆盖手填）：
  - 「适用范围」← 产品 `scope`；「产品描述」← 产品名称/型号/完整版本。
- 「初始化模版」按钮加载标准目录结构。

---

## 3. 章节结构（`content.sections[]` 多级树）

> 封面（cover）/ 文件修订记录（revision）为前置章节，不参与正文编号。

```text
网络安全风险管理计划                    ← ref_type=cover（封面信息表）
文件修订记录                            ← ref_type=revision
1 概述
   1.1 目的
   1.2 产品描述
   1.3 适用范围
   1.4 系统架构和安全实现
      1.4.1 系统总体架构                  ← ref_type=flow_diagram（自动获取网络安全流程图）
2 阶段活动                              ← ref_type=stage_activity（阶段/开始/结束/结果 表）
3 关联文件
4 网络安全风险管理小组                     ← ref_type=team（按项目角色自动匹配人员）
5 网络安全风险管理计划
   5.1 网络安全风险管理流程
   5.2 网络安全风险管理活动
      5.2.1 网络安全威胁识别
      5.2.2 网络安全风险评估
      5.2.3 网络安全风险控制
      5.2.4 网络安全风险评审
   5.3 网络安全风险管理可接受水平
6 网络安全风险管理资源、培训和沟通
   6.1 资源
   6.2 培训
   6.3 沟通
7 网络安全风险管理评审
8 网络安全风险管理文件与记录
```

---

## 4. 前端实现

| 路径 | 说明 |
|------|------|
| `pages/cybersec_plan/CybersecPlanDocs.tsx` | 列表 |
| `pages/cybersec_plan/CybersecPlanDocDetail.tsx` | 详情（章节树编辑） |

> 一级菜单 **「开发测试文件管理 → 测试文件」** 下挂 **「网络安全风险管理计划」**，路径 `/cybersec_plan_docs`。

---

## 5. 后端接口（`/cybersec_plan_doc`）

| 方法 | 路径 | 权限 | 入参要点 |
|------|------|------|----------|
| POST | `/add_cybersec_plan_doc` | `cybersec_plan_doc_edit` | product_id, version, file_no?, change_log?, content? |
| GET | `/duplicate_cybersec_plan_doc` | `cybersec_plan_doc_edit` | id, product_id?（目标产品；跨产品复制时新版本按目标产品最大版本递增） |
| POST | `/update_cybersec_plan_doc` | `cybersec_plan_doc_edit` | 含 id；content 经 normalize |
| DELETE | `/delete_cybersec_plan_doc` | `cybersec_plan_doc_edit` | id |
| GET | `/list_cybersec_plan_doc` | `cybersec_plan_doc_view` | product_id?, version? 模糊 |
| GET | `/get_cybersec_plan_doc` | `cybersec_plan_doc_view` | id |
| GET | `/export_cybersec_plan_doc` | `cybersec_plan_doc_view` | id |

---

## 6. 数据库表

`cybersec_plan_doc`（单表，content JSON）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | 自增主键 |
| product_id | Integer NOT NULL | 产品ID |
| version | String(64) NOT NULL | 文档版本 |
| file_no | String(128) | 文件编号 |
| change_log | String(512) | 版本变更说明 |
| content | JSON | 文档内容（章节树） |

唯一约束：`(product_id, version)`

content 结构：`sections[]`（title, ref_type, text, tables, children, images）、productName。

ref_type 取值：cover、revision、stage_activity、flow_diagram、team。

---

## 7. 关键业务逻辑与规则

1. **计划文档**：与网络安全风险管理报告（47）配套，本模块是计划阶段，47 是执行结果文档。两者独立模型、独立表，零耦合。
2. **自动获取**：
   - 封面日期/签署人从产品时间线和参与人员获取。
   - 4. 网络安全风险管理小组：按项目角色关键字（如"网络安全负责人"、"产品经理"等）从产品参与人员自动匹配填充。
   - 1.4.1 系统总体架构（`ref_type=flow_diagram`）：自动获取产品网络安全流程图（同 47 规则）。
3. **复制（支持跨产品）**：列表「复制」弹窗选目标产品（默认当前产品），版本号自动生成、不允许手动填。
   - 同产品：`new_version(原版本)` 递增；跨产品：按目标产品现有最大版本 `new_version()` 递增。
4. **删除文档**：直接删除，无级联子表。
5. **导出 Word**：封面「网络安全风险管理计划」→ 封面信息表 → 文件修订记录 → 目录(TOC) → 正文章节（带章节号，含正文表格与正文图片）。

---

## 8. 修改注意事项 / 不可破坏的核心逻辑

1. 与网络安全风险管理报告（47）独立并行，不可复用其模型与表。
2. 自动获取章节不可编辑。
3. 封面/修订记录为前置章节，不参与正文编号。
