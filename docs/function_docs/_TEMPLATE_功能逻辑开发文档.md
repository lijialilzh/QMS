<!--
============================================================
《功能逻辑 & 开发文档》全局模板（强制标准）
============================================================
适用范围：本目录下所有「功能逻辑 & 开发文档」必须严格按本模板的 8 个固定模块输出。

【使用方法】
1. 复制本文件，重命名为「<编号>_<功能名>.md」（例如 25_变更需求表删除.md）。
2. 删除本段注释与各模块下的「填写说明（斜体灰字）」，替换为真实内容。
3. 8 个模块标题、顺序、编号一律不得删减、不得改名、不得简化、不得调序。
4. 若某模块确无内容，写「无」并说明原因，不得直接删掉该模块。

【写作硬性要求】
- 语言直白、逻辑线性，步骤用 1./2./3. 连续编号，分支用「分支A/分支B」明确标出走向。
- 完全脱离代码也能看懂：所有判断、触发条件、执行步骤、数据流向、异常场景逐条拆解。
- 不贴大段代码；必要时只标注关键函数名 / 文件路径（如 `serv_srs_type.py::delete_srs_type`）。
- 每条规则写「规则原文 / 触发条件」+「含义 / 后果」，不能只给结论。
- 涉及数据库表、接口字段，引用本目录 01/02~06 文档，不重复粘贴大表。
============================================================
-->

# <功能名称> 功能逻辑 & 开发文档

> 文档元信息（每次修改必须更新）
>
> | 项 | 内容 |
> |----|------|
> | 功能编号 | <如 25> |
> | 所属模块 | <如 SRS 需求规格说明> |
> | 主要代码位置 | <前端文件 / 后端文件 / 接口，逗号分隔> |
> | 文档版本 | v1.0 |
> | 最后更新 | <YYYY-MM-DD> |

---

# 1. 功能概述

*填写说明：用 3~6 句话讲清楚——*
- **用途**：这个功能解决什么问题。
- **使用场景**：用户在什么页面、什么情况下会用到。
- **最终目标**：执行成功后系统应达到的状态。
- **关联上下游功能**：它依赖谁、谁依赖它（列出上游触发来源与下游受影响功能）。

---

# 2. 前置依赖 & 关联模块

*填写说明：逐类列全，并对不可改动的部分打标记。*

## 2.1 涉及页面
| 页面 / 路由 | 作用 |
|------------|------|
| <路径> | <说明> |

## 2.2 涉及组件
| 组件 / 文件 | 作用 |
|------------|------|
| <路径> | <说明> |

## 2.3 涉及接口
| 方法 | 路径 | 权限 | 作用 |
|------|------|------|------|
| <GET/POST/DELETE> | <路径> | <perm> | <说明> |

## 2.4 状态仓库 / 公共方法
| 名称 | 类型（state/util/helper） | 作用 |
|------|--------------------------|------|
| <名称> | <类型> | <说明> |

## 2.5 【只读 / 禁止修改】模块清单
*填写说明：明确列出本功能涉及但绝对不能改的文件 / 函数 / 目录（公共状态、基础组件、`src/store`、`src/common`、`src/api` 等），并说明为何只读。*
| 模块 / 文件 / 函数 | 标记 | 原因 |
|--------------------|------|------|
| <路径> | 【只读/禁止修改】 | <原因> |

---

# 3. 完整执行流程（分步细化）

*填写说明：从「触发入口」开始，严格按真实执行顺序逐步写。每一步必须包含 6 要素：*
*动作 → 入参 → 内部逻辑 → 数据变化 → 调用链路 → 页面表现。*
*遇到任何 if / 判断 / 多选 / 多入口，必须单独拆「分支」，写清每个分支的条件与走向。*

## 3.0 触发入口
*列出所有能触发本功能的入口（按钮、菜单、自动触发等）及其对应的初始动作。*

## 3.1 步骤 1：<动作名>
- **动作**：<用户/系统做了什么>
- **入参**：<字段名:含义，来源>
- **内部逻辑**：<这一步内部如何处理>
- **数据变化**：<内存状态 / DB 表 行的变化>
- **调用链路**：<前端函数 → 接口 → 后端 serv 函数>
- **页面表现**：<用户看到什么>

### 分支 1A：<条件原文>
- 走向：<满足该条件时如何执行>
### 分支 1B：<条件原文>
- 走向：<否则如何执行>

## 3.2 步骤 2：<动作名>
*（同上 6 要素 + 分支）*

*（按需继续 3.3、3.4 …，直到流程结束。结尾写明「成功终态」与「数据最终落库结果」。）*

---

# 4. 核心规则 & 判定条件

*填写说明：把流程中出现的所有布尔判断、过滤规则、权限校验、状态判断逐条抽出。*
*每条写「规则原文 / 判定条件」+「含义」+「不满足时的后果」。*

| 编号 | 规则 / 判定条件（原文或精确描述） | 含义 | 不满足 / 命中时的后果 |
|------|----------------------------------|------|----------------------|
| R1 | <如：typeId 必须为 0<typeId<1e9 的真实库 ID> | <区分真实 ID 与临时 ID> | <临时 ID 走本地删除分支，不调接口> |
| R2 | <权限：perm = srs_doc_edit> | <编辑权限> | <无权限返回 code=0, msg_no_perm> |
| … | | | |

---

# 5. 数据结构说明

*填写说明：列出本功能读写的核心字段。引用 01/03~06 文档中的表/对象，不重复整表，只列与本功能相关的字段。*

| 字段 | 所属（表/对象/状态） | 含义 | 默认值 | 取值范围 | 流转规则（谁写、谁读、何时变化） |
|------|---------------------|------|--------|----------|-------------------------------|
| <字段> | <来源> | <含义> | <默认> | <范围> | <流转> |

*若有跨层数据流（前端 state → 接口入参 → DB → 接口出参 → 前端刷新），用一段「数据流向」文字串起来。*

---

# 6. 异常场景 & 兜底逻辑

*填写说明：穷举异常并写处理方式。至少覆盖：报错、为空、重复、删除/编辑失败、并发/刷新、临时数据未就绪、权限不足、网络异常。*

| 场景 | 触发条件 | 系统处理 / 兜底 | 用户提示文案 |
|------|----------|----------------|--------------|
| <为空> | <如目标对象不存在> | <如返回 msg_obj_null，前端回滚乐观更新> | <提示原文> |
| <重复> | <唯一约束冲突> | <…> | <…> |
| <删除失败> | <…> | <…> | <…> |
| … | | | |

---

# 7. 功能约束 & 修改红线

*填写说明：必须明确写死下列三类边界，作为后续开发/修复的硬约束。*

## 7.1 允许修改范围
- <仅允许在以下文件/函数内修改：…>

## 7.2 绝对禁止改动（红线）
- **禁止改动的逻辑 / 函数 / 文件**：<逐条列出，如 `serv_srs_type.py::__matches_change_table_title` 的匹配规则>
- **禁止改动的判定条件 / 数据规则**：<如唯一约束、type_code 语义、临时 ID 阈值 1e9>

## 7.3 禁止新增逻辑
- <不得新增文档未描述的分支 / 字段 / 接口>

## 7.4 禁止重构结构
- <不得整文件重写、不得合并/重命名/调整既有函数结构>

---

# 8. 验收标准

*填写说明：逐条列出可勾选的验收点。满足全部即功能正常，任一不满足即为问题。*

- [ ] AC1：<可观测、可验证的预期结果>
- [ ] AC2：<…>
- [ ] AC3：<边界场景的预期（如删除后刷新不复活）>
- [ ] AC4：<异常场景的预期（如无权限被拦截）>
- [ ] AC5：<不应产生的副作用（如其它数据未被误删）>
